{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run tests",
            "type": "python",
            "request": "launch",
            "module": "unittest",
            "args": ["discover", "-s", "tests", "-v"]
        }
    ]
}
